# Workspace Rules - QualiNABH Error Checking

## Startup Validation
- **Lint Check on Startup**: When you initialize a new session or open this workspace in Antigravity, immediately run `npm run lint` (or `npm.cmd run lint` if on Windows) to verify if any new linter errors or warnings have been introduced, and report the status.
